import React from "react";
// import { toast } from "react-toastify";

const Form = ({
  setRefresh,
  refresh,
  selectedUser,
  setSelectedUser,
  open,
  closeForm,
}) => {
  return (
    <div className=" fixed bg-[55555555] left-0 top-3 right-0 bottom-0">
      <h1 className="text-2xl font-bold flex justify-center">Add Record</h1>
    </div>
  );
};

export default Form;
